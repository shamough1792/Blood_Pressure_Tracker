<%- include('admin-start') %>
<div class="admin-grid"><section class="admin-panel"><h2>匯出 SQL 備份</h2><p>備份包含所有使用者與完整血壓記錄，可保存作為還原檔。</p><div class="admin-tool"><strong>完整資料庫備份</strong><span>系統會產生一個 SQL 檔案供下載。</span><a href="/admin/export/sql" class="btn btn-primary">下載 SQL 備份</a></div></section><section class="admin-panel"><h2>匯入 SQL 記錄</h2><p>將備份檔中的記錄匯入指定使用者。匯入後不會覆蓋現有資料。</p><div class="admin-tool admin-import"><label for="importUser">指定使用者</label><select id="importUser" <%= users.length ? '' : 'disabled' %>><% users.forEach(function(u) { %><option value="<%= u.id %>"><%= u.name %></option><% }); %></select><form id="importForm"><label class="sr-only" for="sqlFile">SQL 備份檔案</label><input type="file" id="sqlFile" accept=".sql" required <%= users.length ? '' : 'disabled' %>><button type="submit" id="importBtn" class="btn btn-primary" <%= users.length ? '' : 'disabled' %>>上傳並匯入</button></form><div id="importResult" class="admin-result" role="status" hidden></div></div><% if (!users.length) { %><div class="admin-empty">請先建立使用者，再指定匯入對象。</div><% } %></section></div>
<%- include('admin-end') %>
